'use strict';

const { expect } = require('chai');
const fp = require('../lib/fp.js');

describe('map function'), function (){
  let result = sut.mapFunction([1,2,3], (e) => e*2)
  it('should return an array'), function(done){
    
  }
};
